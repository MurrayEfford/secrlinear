############################################################################################
## package 'secrlinear'
## branched.R
## 2022-11-14
############################################################################################

branched <- function (mask) {
  if (inherits(mask, "linearmask")) {
    any(degree(attr(mask, 'graph'))>2)
  }
  else {
    NA
  }
}
